import TransactionHeader from "./TransactionHeader/TransactionHeader";
import Section from "../_share/Section/section";

const TransactionPage = () => {
  return (
    <Section>
      <TransactionHeader transTitle="transTitle" />
    </Section>
  );
};

export default TransactionPage;
